spring:
  application:
    name: turbine
  cloud:
    config:
     uri: http://localhost:8888
server:
  port: 5678
turbine:
  appConfig: reservation-client,reservation-client-1,reservation-client-2
  clusterNameExpression: "'default'"
  combine:
    host:
     port: true